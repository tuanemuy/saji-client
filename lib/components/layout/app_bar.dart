import 'package:flutter/material.dart';

final appBar = AppBar(
    title: const Text('Saji'),
    titleTextStyle: const TextStyle(
        fontSize: 16, fontWeight: FontWeight.w600, fontFamily: 'LINESeed'));
